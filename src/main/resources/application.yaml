server:
  port: 8881

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/social_app
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
